# Server port (frontend default expects 3001)
PORT=3001

# Allowed CORS origin for frontend (Vite default)
CORS_ORIGIN=http://localhost:5173

# Path to logs JSON file (relative to backend root; optional)
# DATA_FILE=data/logs.json
